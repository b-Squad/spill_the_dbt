version: 2

models:
  - name: lego
    description: "initial test for checking schema"
    columns:
      - name: theme_name
        data_tests:
          - not_null
      - name: set_name
        data_tests:
          - not_null
      - name: set_year
        data_tests:
          - not_null
